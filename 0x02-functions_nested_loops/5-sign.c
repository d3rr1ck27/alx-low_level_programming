#include "main.h"

/**
 * print_sign - prints the operator sign of number
 * @n: the int to check
 * Return: 1 and prints + if n >  zero
 * 0 and prints 0 if n == zer0
 * -1 and prints - if n < zero
 */
int print_sign(int n)
{
	if (n > 0)
	{
		_putchar('+');
		return (1);
	} else if (n == 0)
}
