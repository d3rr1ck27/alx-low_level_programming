#include "main.h"
/**
 * _islower - checls for lowercase character
 * @c: the character to checl
 * Return: 1 if c is lowercase, 0 otherwise
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
